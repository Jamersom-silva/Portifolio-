@page
@model MeuPortfolio.Pages.SobreModel
@{
    ViewData["Title"] = "Sobre Mim | Jamerson Silva";
}

<style>
    /* Estilos do fundo */
    .background-image {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('/images/main.jpg') no-repeat center center;
        background-size: cover;
        z-index: -2;
        filter: brightness(0.8); /* Igual ao da página de contato */
    }

    /* Fundo escuro semi-transparente - MESMO VALOR DO CONTATO (0.185) */
    .dark-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.185); /* Exatamente igual ao da página de contato */
        z-index: -1;
    }

    .content-container {
        position: relative;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 40px 20px;
        color: white;
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }

    /* Container do conteúdo */
    .profile-content {
        display: flex;
        max-width: 1000px;
        width: 100%;
        gap: 40px;
        align-items: center;
        background: rgba(255, 255, 255, 0.05); /* Transparência igual ao contato */
        backdrop-filter: blur(10px);
        border-radius: 10px;
        padding: 40px;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    /* Imagem em tamanho completo sem bordas */
    .profile-image {
        width: 400px;
        height: auto;
        max-height: 500px;
        object-fit: contain;
        border-radius: 8px;
    }

    .text-content {
        flex: 1;
    }

    /* Tipografia */
    h1 {
        color: #ffffff;
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        font-weight: 700;
        letter-spacing: -0.5px;
        line-height: 1.2;
    }

    .about-text {
        line-height: 1.7;
        margin-bottom: 1.8rem;
        font-size: 1.15rem;
        font-weight: 400;
        color: rgba(255, 255, 255, 0.9);
    }

    .goals-box {
        background: rgba(255, 255, 255, 0.08); /* Transparência similar */
        padding: 2rem;
        border-radius: 8px;
        border-left: 3px solid rgba(255, 255, 255, 0.2);
    }

    h2 {
        color: #ffffff;
        font-size: 1.8rem;
        margin-bottom: 1.2rem;
        font-weight: 600;
        line-height: 1.3;
    }

    .goals-box p {
        line-height: 1.7;
        font-size: 1.1rem;
        color: rgba(255, 255, 255, 0.85);
    }

    /* Responsividade */
    @@media (max-width: 992px) {
        .profile-content {
            flex-direction: column;
            text-align: center;
            padding: 30px;
        }
        
        .profile-image {
            width: 100%;
            max-width: 300px;
            margin-bottom: 2rem;
        }
        
        h1 {
            font-size: 2.2rem;
        }
        
        h2 {
            font-size: 1.6rem;
        }
    }

    @@media (max-width: 576px) {
        .profile-content {
            padding: 25px 20px;
        }
        
        h1 {
            font-size: 2rem;
        }
        
        .about-text, .goals-box p {
            font-size: 1rem;
        }
    }
</style>

<!-- Fundo -->
<div class="background-image"></div>
<div class="dark-overlay"></div> <!-- Adicionei esta linha para o overlay igual ao contato -->

<!-- Conteúdo principal -->
<div class="content-container">
    <div class="profile-content">
        <!-- Imagem em tamanho completo -->
        <img src="/images/profile.jpg.svg" class="profile-image" alt="Jamerson Silva">
        
        <!-- Texto -->
        <div class="text-content">
            <h1>Sobre Mim</h1>
            <p class="about-text">
                Desenvolvedor Back-End com 1 ano de experiência em projetos freelancer.<br>
                Especializado em criar soluções robustas usando tecnologias modernas.
            </p>
            
            <div class="goals-box">
                <h2>Objetivos Futuros</h2>
                <p>
                    Meu entusiasmo pelo desenvolvimento web me leva a buscar conhecimentos mais profundos. 
                    No futuro, pretendo expandir minhas habilidades para o desenvolvimento de back-end, 
                    especialmente com Node.js. Estou ansioso para aprender mais sobre como criar e dar vida 
                    a projetos ainda mais ambiciosos.
                </p>
            </div>
        </div>
    </div>
</div>